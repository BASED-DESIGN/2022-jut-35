/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  distDir: 'build',
  // assetPrefix: ".",
  // images: {
  //   loader: "imgix",
  //   path: "./",
  // },
}

module.exports = nextConfig
